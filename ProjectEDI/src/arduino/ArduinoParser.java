package arduino;

import org.jfugue.parser.Parser;

public class ArduinoParser extends Parser {
	//Quick thought. plan to add play, pause, rewind, fast forward, maybe scroll through song..
	//Need to talk about these options.
	//To be able to handle those options, a queue would not support unless we kept two copies, 
	//one full and one playing. Might need to investigate alternative options
	
	// TODO: Handles jfugue pattern -> Arduino Pattern
}
